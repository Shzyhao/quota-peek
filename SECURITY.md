# 安全说明

## 报告漏洞

发现安全问题请**不要开公开 Issue**，通过 GitHub 私密安全公告（Security → Report a vulnerability）或邮件联系仓库所有者，收到后会在 7 天内回应。

## 范围内的关注点

- API Key / 凭证的存储、脱敏与日志净化链路（本工具处理用户密钥，这是最高优先级）
- 供应商响应体的解析（XSS / 注入，界面全部纯文本渲染，不允许 innerHTML 直插响应内容）
- 备份导入的 JSON 解析（结构校验、覆盖式导入的确认流程）

## 不在范围内

- 用户自行把备份 JSON（含明文 Key）分享给他人造成的泄露
- 供应商官方接口本身的安全问题（请直接反馈给对应供应商）
- 未加锁的本地电脑上任何本地数据被同机程序读取（纯本地方案的固有边界，桌面版规划中有迁移 Windows 凭据管理器的路线）

## 设计要点（给审计者）

- 密钥仅存本机（浏览器 localStorage / WebView2 存储），不上传任何服务器
- 界面展示首尾 4 位脱敏；编辑留空表示沿用原密钥，从不回显明文
- 日志结构无密钥字段，错误信息写入前统一净化（剔除 `sk-` 形式的 token）
- 仓库内所有密钥均为测试占位值
