{
  "name": "model-quota-desktop",
  "private": true,
  "version": "0.1.0",
  "description": "看额度（原：模型额度查询）- Tauri 2 桌面壳，前端复用 model-quota-frontend",
  "scripts": {
    "tauri": "tauri",
    "desktop:dev": "tauri dev",
    "desktop:build": "tauri build --no-bundle && cp src-tauri/target/release/model-quota-app.exe \"src-tauri/target/release/看额度.exe\"",
    "desktop:dist": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
