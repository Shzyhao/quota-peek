{
  "name": "model-quota-desktop",
  "private": true,
  "version": "0.5.0",
  "description": "桌看（原：看额度）- Tauri 2 桌宠壳，前端复用 model-quota-frontend",
  "scripts": {
    "tauri": "tauri",
    "desktop:dev": "tauri dev",
    "desktop:build": "tauri build --no-bundle && node scripts/copy-exe.mjs",
    "desktop:dist": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
