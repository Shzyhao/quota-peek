# 待真实测试清单（桌看 v0.9.1）

> 创建：2026-09-15 · 对应版本：v0.9.1（ZhuoKan-v0.9.1.exe）
> 说明：以下各项在自动化验证中已用 **mock 语音服务 + 合成输入** 跑通（E2E 11/11、菜单专项 5/5、单元测试 264/264），
> 但**真实麦克风 / 真实 API Key / 真机交互**尚未人工验证。逐项测试后勾选，发现问题记到每项下方。

## 前置条件

- [ ] 安装 v0.9.1 exe（GitHub Release 下载 `ZhuoKan-v0.9.1.exe`，或本地 `src-tauri/target/release/桌看-v0.9.1.exe`），确认托盘图标出现、版本为 0.9.1
- [ ] 硅基流动（或任意 OpenAI 兼容语音服务）API Key 一个 → https://cloud.siliconflow.cn 注册即送额度
- [ ] Windows 设置 → 隐私和安全性 → 麦克风：「麦克风访问」开 +「允许桌面应用访问麦克风」开

## 一、语音对话（P0 · 核心新功能）

配置：对话页 →「模型配置」→ 展开「语音服务」卡 → 确认默认硅基流动配置 → 填入 API Key → 保存（提示"Key 已写入凭据管理器"）→ 状态显示「✓ Key 已存」

- [ ] 1.1 **试听**：点「🔊 试听」→ 数秒内听到女声播"你好呀主人，我是你的桌宠，语音服务一切正常！"，音色自然（CosyVoice2-anna）
  - 预期：试听完成后提示「✓ 试听播放完成」
- [ ] 1.2 **点按说话全链路**：点输入栏 🎙 → 对桌宠说一句中文（如"今天天气怎么样"）→ 再点 ⏹ → 气泡/按钮短暂显示识别中 → 识别文字自动填入并发送 → 模型流式回复 → 回复完成后自动朗读
  - 预期：整链路无感串联，朗读内容与回复文字一致（无 Markdown 符号被念出来）
- [ ] 1.3 **识别准确率**：连测 5 句不同长度（3 字短语 / 日常长句 / 含数字 / 含英文单词），记录识别正确的句数：___/5
- [ ] 1.4 **桌宠入口**：点击桌宠 → 菜单「🎙 语音对话」→ 对话面板弹出并**自动进入录音态**（🎙 变红色 ⏹）→ 直接说话 → 松开后再点 ⏹ → 正常走完链路
- [ ] 1.5 **朗读开关**：关掉 🔊 朗读 → 发消息 → 回复不再朗读；重新打开 → 恢复朗读；朗读进行中关掉开关 → 立即停止播放
- [ ] 1.6 **打断**：朗读进行中点 🎙 → 朗读立刻停止并进入录音；朗读中直接打字发送 → 朗读停止
- [ ] 1.7 **桌宠联动气泡**：说话时桌宠气泡「🎤 主人请讲」→ 识别时「让我听听」→ 回复时「让我想想哈」→ 朗读时「我来念给你听」，结束后气泡自动收起
- [ ] 1.8 **边界**：点一下立刻点停（<0.4s）→ 提示"录音太短啦"；持续说话超 60 秒 → 自动停止并识别前 60 秒；安静环境不说话 → 提示"没听清你说什么"

### 异常路径

- [ ] 1.9 Key 删除后点 🎙 → 提示引导去「语音服务」卡配置
- [ ] 1.10 Key 故意改错一位保存 → 点 🎙 说话 → 报错信息可读（HTTP 401 类，非乱码）
- [ ] 1.11 断网点 🎙 → 报错提示"连接失败/超时"，恢复网络后可正常使用
- [ ] 1.12 Windows 麦克风总开关关闭 → 点 🎙 → 提示检查 Windows 隐私设置（而非无响应）

## 二、功能菜单位置（P1 · v0.9.1 改动）

- [ ] 2.1 点击桌宠 → 菜单出现在**人物左侧**竖条，人物完整可见不被遮挡（自定义形象/换肤后同样验证一次）
- [ ] 2.2 把桌宠拖到屏幕**最左侧**边缘 → 再点开菜单 → 菜单自动换到人物**右侧**（且不超出屏幕）
- [ ] 2.3 菜单开着**拖动桌宠** → 人物跟随鼠标移动（无跳变）、松手停在松手位置 → 重启应用 → 位置还原为松手位置（不是旧位置）
- [ ] 2.4 菜单开着拖动后直接点 × 关闭 → 窗口正常收回，桌面不留透明点击死区（在原菜单条区域点桌面图标可选中）
- [ ] 2.5 菜单展开时逐项点击：「💬 对话」「📄 文件分析」「📊 额度速览」「👗 换装」「我的形象」各功能正常

## 三、多窗口与恢复（P2）

- [ ] 3.1 主窗对话页和桌宠面板**同时开着**，两边各有一次语音交互 → 各自正常、互不抢麦
- [ ] 3.2 语音对话产生的会话在主窗/面板间同步正常（无重复、无丢失）
- [ ] 3.3 关机重启 → 桌宠位置还原正确（菜单展开后收起过的位置不算错位）

## 四、观察项（P2 · 非测试，日常留意）

- [ ] 4.1 页面文件修复后（系统托管已开启，2026-09-15）：ZCode / 构建等重负载场景数天内是否还出现"闪退"或内存分配失败
- [ ] 4.2 桌宠长时间挂机（数小时）显存/内存是否稳定（自动化已测 6.5 分钟走平）
- [ ] 4.3 语音功能的使用成本：硅基流动控制台看当日语音调用的实际扣费（预期识别免费、合成几分钱内）

---

## 测试记录

| 日期 | 版本 | 项 | 结果 | 备注 |
|---|---|---|---|---|
| | | | | |
