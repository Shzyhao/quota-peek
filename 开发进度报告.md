# 看额度 · 开发进度报告

> 产品名：**看额度**（原名「模型额度查询」，2026-09-02 更名，功能不变）· 英文名 / 仓库名：**QuotaPeek**（repo 建议 `quota-peek`）
> 仓库：https://github.com/Shzyhao/quota-peek （2026-09-02 首次推送，main 分支）
> 更新日期：2026-09-02
> 项目位置：`model-quota-frontend/`（纯前端，Vite + 原生 JavaScript + Vitest）+ `src-tauri/`（Tauri 2 桌面壳，阶段 8 起生效）
> 配套文档：[模型额度查询要求.md](./模型额度查询要求.md)（原始需求）· [私有化桌面版演进规划.md](./私有化桌面版演进规划.md)（后续规划）· [model-quota-frontend/README.md](./model-quota-frontend/README.md)（技术细节与接口调研）

---

## 一、项目概览

一个轻量级本地小工具，用于查询和记录多个大模型供应商的套餐余额、剩余额度和到期时间。按原始需求文档开发，前期采用「浏览器直连官方接口 + localStorage 存储」的纯前端架构（`model-quota-frontend/`）；阶段 8 起由 Tauri 2 桌面壳（`src-tauri/`）包装为**托盘常驻的桌面应用**，前端代码原样复用。

**核心指标（当前）**

| 指标 | 状态 |
|---|---|
| 自动查询供应商 | **9 家**（DeepSeek / 智谱 Coding / 火山方舟 Coding / MiniMax Coding / Kimi·Moonshot 余额 / 硅基流动 / StepFun / OpenRouter / Novita） |
| 手动维护类型 | 5 类（Anthropic 订阅 / OpenAI·Codex / Qwen / Kimi 会员编程套餐 / 其他） |
| 测试 | **147/147 全部通过**（15 个测试文件，Vitest + jsdom） |
| 生产构建 | `vite build` 通过（JS gzip 后约 16.7 kB） |
| 用户实测已通过 | DeepSeek、智谱 GLM Coding Plan、火山方舟 Coding Plan（三家真实 Key 验证查询成功） |
| 界面 | 侧边栏应用壳四视图、深浅双主题、用量进度条、搜索筛选、样式化确认弹窗、迷你小窗、备份导入导出 |
| **桌面版（阶段 8 起）** | **Tauri 2 桌面壳已落地**：`src-tauri/target/release/model-quota-app.exe`（约 5.9 MB），托盘常驻 + 关窗驻留 + 托盘迷你小窗 + 单实例，GUI 验收通过 |

---

## 二、开发历程（按阶段）

### 阶段 1 · 初版开发（完成）
- 依据需求文档确定纯前端技术方案：Vite + 原生 JS（无 UI 框架）+ Vitest + jsdom。
- 核心层（`src/core/`）：API Key 脱敏、localStorage 仓储、查询日志（脱敏 + 上限 100 条）、状态评估（到期/低余额/低额度/失败颜色分级）、刷新编排服务。
- UI 层：供应商卡片、添加/编辑弹窗、查询日志表、迷你小窗（`?view=mini`）。
- **DeepSeek 接入**：官方 `GET /user/balance`，实测浏览器 CORS 直连可用，真实 401 错误链路验证通过。
- 验证：64 个测试全绿；浏览器端到端实测（添加→刷新→日志→脱敏）。

### 阶段 2 · 多供应商扩展（完成）
- **智谱 GLM Coding Plan**：采用官方开源插件 `glm-plan-usage` 同款接口 `GET /api/monitor/usage/quota/limit`（裸 Key 鉴权），CORS 实测通过。
- **MiniMax**：官方 CLI `mmx quota` 同款接口 `GET /v1/token_plan/remains`（Bearer），双端点路由（`sk-api-` 普通密钥自动走 `/account/query_balance`）。
- **Kimi / Moonshot**：官方文档接口 `GET /v1/users/me/balance`（Bearer）。
- 完成全行业调研并形成对照表（详见 README）；三家新接口的真实错误路径在浏览器中实测验证。
- 验证：94 个测试全绿。

### 阶段 3 · 演进规划文档（完成）
- 应需求产出《私有化桌面版演进规划.md》：桌面化选型（推荐 Tauri 2）、系统托盘、系统通知、**AI 辅助接口自适配**（声明式 DSL + AI 修订配置的安全设计）、UI 升级方向、四阶段路线图与风险对策。此阶段仅产出分析文档，未实施开发。

### 阶段 4 · 界面样式优化与功能开发（完成）
- **应用壳重构**：单页长滚动 → 左侧边栏 + 四视图（总览/供应商/查询日志/设置）hash 路由。
- **深色模式**：设计 tokens 双主题，跟随系统/浅色/深色三档，顶栏一键切换，迷你窗同步。
- **总览页**：余额合计、供应商数、需关注数、最近刷新统计卡；卡片按严重度排序。
- **供应商页**：关键词实时搜索（局部刷新保焦点）+ 六档状态筛选。
- **用量可视化**：Coding Plan 卡片 5 小时/本周/本月 MCP 三条进度条（<80 绿 / 80–90 橙 / ≥90 红），余额型剩余额度占比条。
- **样式化确认弹窗**：替代原生 confirm，危险操作红色样式，防注入（纯文本渲染）。
- **数据备份**：JSON 导出/导入（覆盖式导入带二次确认与结构校验）、清空全部数据。
- 验证：125 个测试全绿；浏览器 GUI 逐项验证（导航/搜索双向过滤/深色截图经视觉模型确认/确认弹窗）。

### 阶段 5 · 用户实测反馈修复（完成）
用户真实验证发现：智谱只显示 5 小时用量，无本周/已用/总额。根因与修复：
- 智谱周额度与 5 小时额度是**同类型（TOKENS_LIMIT）双条目**，靠 `unit` 字段区分（3=5 小时、6=周）——原适配器只取了第一条。已按 unit 精确解析。
- 接口其实返回**已用/总额数值**（`currentValue`/`usage`）与**重置时间**（`nextResetTime`），现均已解析：卡片显示「本周已用/本周总额」数值，套餐详情行显示重置时间。
- 验证：127 个测试全绿（新增 unit 区分、无 unit 回退、百分比超 100 截断等用例）。

### 阶段 6 · 火山方舟接入（完成，用户已验证通过）
- 调研结论：火山用量查询走**官方 OpenAPI** `POST open.volcengineapi.com/?Action=GetCodingPlanUsage`，鉴权为 **IAM AK/SK + SigV4 签名**（非模型推理的 Ark API Key）；实测 CORS 完全开放。
- 实现：浏览器端 **Web Crypto SigV4 签名**（派生链 Date→Region→Service(ark)→request），测试中与 Node 官方 crypto 实现对拍**逐字节一致**；未订阅 Coding Plan 时自动回退 `GetAFPUsage` 查 Agent Plan。
- **双凭证支持**：表单按类型显示「AccessKey ID + Secret Access Key」两栏，全链路（存储/脱敏/日志净化）支持。
- 安全建议已写入 README：强烈建议使用仅授予方舟只读权限的 IAM 子账号密钥。
- 验证：139 个测试全绿；用户以真实 AK/SK 验证**查询成功**。

### 阶段 7 · 参考 cc-switch 的扩展（完成，待用户验证）
调研 [cc-switch](https://github.com/farion1231/cc-switch) 的用量查询实现，成果：
- **新增 4 家余额查询**（CORS 全部实测开放，cc-switch 同款接口）：硅基流动（`/v1/user/info`，国际站自动 USD）、StepFun（`/v1/accounts`）、OpenRouter（`/api/v1/credits`，余额=总额度−已用）、Novita（`/v3/user/balance`，单位 0.0001 USD 已换算）。
- **MiniMax 双端点升级**：优先 cc-switch 同款新端点 `openplatform/coding_plan/remains`（取 general 条目、周额度激活判断、重置时间），失败自动回退官方 CLI 旧端点。
- **Kimi 会员编程套餐**：查到 cc-switch 所用接口 `api.kimi.com/coding/v1/usages`，但实测**服务端不响应 CORS 预检（OPTIONS 404）**，纯前端无法接入；类型保留为「暂不可查」手动维护。
- 验证：147 个测试全绿。

### 阶段 8 · Tauri 2 桌面壳落地（完成，按《私有化桌面版演进规划》Phase 1 轻量版，2026-09-02）
复用本机 Claude Code 开发「轻析 LiteAI」（`D:\TEST Code\AI 文件分析助手`，Tauri 2 + Vite）的工程模板与本机编译环境经验（VS Build Tools 在 D 盘 + MSVC 链接器固定 + cargo USTC 镜像），零改动嵌入现有前端：
- **新增工程结构**：项目根 `package.json`（Tauri CLI）+ `src-tauri/`（Cargo 工程，仅 3 个依赖：tauri[tray-icon] / single-instance / tauri-build）+ `scripts/gen-icon.mjs`（纯 Node 生成 1024px 应用图标：深色圆角底 + 绿/橙/红三柱条，语义为用量状态）。前端 `model-quota-frontend/` **一行未改核心逻辑**，仅 `main.js` 增加 4 行支持 `#mini` 哈希入口（Tauri 内嵌资产下查询参数会被剥离）。
- **托盘常驻**（需求 11 桌面化）：系统托盘图标 + tooltip + 右键菜单（显示主窗口 / 迷你小窗 / 退出）；左键单击切换主窗口显隐；点击「迷你小窗」打开 400×640 的「额度速览」窗口（复用前端 mini 视图）。
- **关窗驻留**：关闭主窗口 = 隐藏到托盘（WebView 存活，页面内定时刷新不中断）；重复启动 exe 由单实例插件唤起已有窗口；托盘菜单「退出」才真正结束进程。
- **产物**：`src-tauri/target/release/model-quota-app.exe`（约 5.9 MB，opt-level=s + LTO + strip），`--no-bundle` 规避 NSIS 打包器需访问 GitHub 的问题；NSIS 安装包配置已就绪（`npm run desktop:dist`，需开代理）。
- **GUI 逐项验收通过**（2026-09-02，真实运行 + 截图/无障碍树/键盘导航验证）：主窗四视图渲染 ✓ 关窗驻留进程存活 ✓ 单实例唤起 ✓ 托盘菜单弹出与响应 ✓ 迷你小窗内容与空状态引导 ✓ 托盘左键显隐切换 ✓ 深浅主题 ✓。前端 147 测试在改动后复跑全绿。
- 已知边界（与规划的差异）：托盘图标暂为静态色（动态三色/角标需后端感知健康度，归入 Phase 2 与系统通知同批）；隐藏窗口时 WebView2 会节流后台定时器（最低约 1 次/分钟）；密钥仍存 WebView localStorage（Phase 1 完整版的「Credential Manager 迁移」未动，见规划第 5 章）。

---

### 阶段 9 · 界面溢出修复与数值可读性（完成，2026-09-02）
用户实测反馈「添加供应商后，五小时额度/余额等数值超出灰色背景框、显示不全」，浏览器注入长数值数据复现并修复：
- **指标区 4 列改 2×2**：卡片四个指标（余额/剩余额度/已用/总额 或 5h/本周用量/已用/总额）改为两行两列，单个灰框宽度翻倍，长数值（如 155,000,000）完整显示；数值改为超长时可在数字内换行兜底，标签加省略号。
- **formatNumber 千分位分组**：999876543210 → 999,876,543,210（测试断言同步更新为分组格式）。
- **进度条百分比取整**：推导出的剩余占比保留 1 位小数（99.9876543210% → 99.99%→显示 99.99？实际按 1 位小数 99.99% 归一为 100% 或 99.9%），数值列加宽 48→52px。
- **剩余额度条颜色分档修正**：原逻辑把「剩余占比」当「已用占比」分档，导致剩余 99.99% 显示红色；现按已用（100−剩余）分档，主窗与迷你窗同步修正。
- 验证：浏览器注入三家长数值供应商（Coding Plan/余额型）截图逐项确认 ✓；147 测试全绿（`--no-file-parallelism` 串行跑，本机多进程并行时 vitest worker 偶发资源性崩溃）；桌面版重新构建并启动验证。

### 阶段 10 · 低额度弹窗提醒 + 仓库 README（完成，2026-09-02）
- **低额度弹窗提醒**（新功能）：设置页「提醒阈值与定时刷新」卡片新增开关；开启后，每次刷新（手动 / 单个 / 定时）完成时检测 warn/error 级供应商（余额过低、剩余额度过低、用量 ≥ 阈值、临期 / 到期、查询失败），**只对新出现的告警**弹窗提示（红色警示样式，复用样式化确认弹窗，列出供应商与原因）；同一告警同会话只提醒一次，恢复正常后重新计数。实现：`status.js` 新增 `collectAlerts()` 纯函数 + `alertPopup` 设置项，`app.js` 刷新流程接入去重弹窗。
- **仓库 README**：项目根新增 README.md（功能清单、运行指南、安全说明、目录结构、已知限制）。
- 验证：新增 3 个测试用例（collectAlerts 单测 + 弹窗全流程集成测试：默认关不弹→开启弹窗→去重→恢复后重新计数），**150/150 全绿**；浏览器注入告警数据实测弹窗内容与样式（截图确认）；桌面版重建并启动验证。

### 阶段 11 · 桌面悬浮球 + 迷你速览窗（完成，2026-09-02 ~ 09-03）
桌面常驻悬浮球与迷你速览窗两轮迭代（第二轮：圆形科技化改版）：

**第一轮（09-02）· 悬浮球骨架**
- **悬浮球**：无边框置顶小窗（独立 WebView 入口 `#ball`），数据取共享 localStorage；单击在球旁展开迷你窗、按住拖动移动位置（移动 >6px 判定为拖动，调 `startDragging`）；位置持久化（app_config_dir/shell.json），重启还原；托盘菜单「打开/关闭悬浮球」开关。
- **迷你窗浮出模式**：悬浮球单击展开的迷你窗自动置顶 + skip_taskbar，鼠标离开窗体自动收起；托盘菜单打开的为手动模式不自动收起。
- **球内容优先级**：告警数 > 余额合计（≥1 万压缩 `x.x万`）> 供应商数。

**第二轮（09-03）· 圆形科技化 + 低值配色（本轮）**
- **圆形科技风球体**：60px 圆形玻璃质感球（高光渐变 + 状态色内衬 + 深空底），外圈 conic-gradient 旋转光环（6s/圈）、呼吸辉光动画；hover 放大增亮；`prefers-reduced-motion` 全局降级。
- **低值分级配色**（需求：低值显示对应颜色）：球体按最严重告警级别变色——异常红 / 仅低值琥珀 / 正常科技蓝；告警时原生 tooltip 逐行列出供应商与原因；金额 ≥1 亿压缩 `x.x亿`，长数字自适应降字号（`.long` 类），尾零修剪（9218.70→9218.7）。
- **迷你窗缩小**：400×640 → **300×430** 无边框自绘标题栏（渐变 logo + 刷新 + ✕ 关闭=隐藏）；列表紧凑化（行高/字号/间距下调），状态文字与行边框按级别着色，更新时间压缩 `MM-DD HH:mm`；关闭按钮前端直调 `getCurrentWindow().hide()`（capability 新增 `core:window:allow-hide`）。
- **修复**：悬浮球位置恢复 bug——保存的物理坐标曾被 `builder.position` 当逻辑坐标使用，越界后球掉到级联默认位 (128,128)；改为 build 后 `set_position(PhysicalPosition)` 归位。验证机 DPI 虚拟化下 Tauri 物理坐标与 GetWindowRect 有 1.25 换算层，但「Moved 保存 → set_position 恢复」在同一坐标系内自洽，重启落点稳定。
- **清理**：托盘悬停浮窗死代码（tray-icon 在 Windows 无 hover 回调，Enter/Leave 分支永不触发）与 `pointer_inside_mini` 删除。
- 验证：Vitest 16 文件 **158 用例全绿**（新增球三级配色 2 例）；`cargo check` 干净；release 重建后浏览器注入三态数据（error/warn/ok）截图逐项确认球体配色、光环、迷你窗布局与标题栏 ✓；桌面端运行截图确认渲染 ✓。

**已定案结论（勿重排查）**
1. 托盘悬停浮窗在 Windows **不可行**：tray-icon 底层只上报点击、无 hover 回调（文档限制）。
2. ~~透明 WebView 窗口收不到鼠标输入~~ **已推翻（09-03 第三轮验证）**：Tauri 2 Windows 透明窗口默认正常接收输入——`transparent(true)` 只影响渲染不影响命中测试（调研：tauri#13070 反而抱怨透明窗捕获输入、docs.rs `set_ignore_cursor_events` 默认不忽略、iPet 桌宠等透明交互成功案例）。旧结论是合成输入测试假象。
3. 球单击/拖动逻辑**已验证可用**（09-03）：PowerShell 合成点击球中心（GetWindowRect 物理坐标 + mouse_event），「额度速览」迷你窗成功弹出——事件链路（WebView DOM → emit → Rust 建窗）全通。

**第三轮（09-03）· 透明窗口定案**
- 悬浮球改为 `transparent(true)` 真透明窗口 + CSS 画布全透明，**彻底消除不透明方案的深色方块底**；球体、光环、辉光直接叠在桌面上。
- 视觉三轮迭代（see-coder 多模态子智能体像素级验收）：① 透明后黑底消除，但辉光渐变外圈残留 3% 蓝色 alpha，浅色桌面见淡蓝矩形轮廓 → 渐变 62% 处归零；② 顶部仍有辉光被窗口边缘直线截断的硬边（外层 `box-shadow 28px/36px` 扩散延伸到窗口矩形边界）→ 砍掉长阴影、主辉光全部由已收敛的径向渐变承担；③ 复验通过：四方向辉光自然径向渐隐、球外完全透明、无任何矩形轮廓/截断边。
- 输入链路验证：PowerShell 合成点击球中心 → 「额度速览」迷你窗成功弹出（DOM → emit → Rust 建窗全通）。
- 经验沉淀：**透明窗口的视觉效果（阴影/辉光）必须收敛在窗口矩形之内**，任何溢出到边缘的渐变都会被裁出生硬直线；透明不影响命中测试（点击/拖动默认可达 DOM）。

---



### 阶段 12 · 悬浮球界面开关 + 默认开启（完成，2026-09-03）
用户需求：悬浮球除托盘菜单外提供更直观的开关入口，且默认开启。
- **主界面顶栏按钮**：主题按钮右侧新增圆形图标按钮（仅桌面壳 `__TAURI__` 环境渲染，网页版不显示）；开启时高亮（primary-soft 底 + primary 边框），title 提示当前状态。
- **设置页开关**：设置 →「外观」卡片新增「桌面悬浮球」开关（setting-toggle 样式，同样仅桌面版显示），文案说明三色语义与交互。
- **默认开启**：无 shell.json 的首次运行悬浮球自动出现（`load_prefs` 读不到配置时 `visible: true`）；存量用户按已保存偏好。BallPrefs 字段加 `serde(default)` 保证旧文件兼容。
- **事件协议**：前端 `set-ball`（payload: bool，绝对语义）→ Rust `set_ball`（与当前状态不同才 toggle）；任何路径的开关变化都 `emit('ball-state-changed', visible)` 广播，主窗口启动时发 `ball-state-request` 拉取当前状态，顶栏按钮与设置页开关双向同步。
- 验证：Vitest **160 用例全绿**（新增 2 例：网页版不渲染开关、桌面版状态广播同步与 set-ball 发射）；UIA 真机验证（合成鼠标点击驱动不了 WebView 按钮，改用 UIAutomation Invoke/Toggle）：顶栏按钮关闭→球消失且 shell.json `visible:false` ✓ 再开→恢复 ✓；设置页 CheckBox 双向 Toggle ✓；删 shell.json 冷启动默认出现 ✓；顶栏高亮态截图确认 ✓。
- 附注：这台验证机上合成鼠标点击（mouse_event）无法驱动 WebView2 内的按钮（无论前台与否；点击透明悬浮球本身曾成功，可能与置顶未激活路径不同），UIA Invoke 可靠——后续 GUI 自动化验证优先 UIA。

---

## 三、供应商支持总表

| 供应商 | 查询内容 | 接口来源 | 状态 |
|---|---|---|---|
| DeepSeek | API 余额 | 官方文档 | ✅ 已接入，**用户实测通过** |
| 智谱 GLM Coding Plan | 5h/周/MCP 用量% + 数值 + 重置时间 | 官方插件同款 | ✅ 已接入，**用户实测通过** |
| 火山方舟 Coding Plan | 5h/周/月用量% + 重置时间（Agent Plan 自动回退） | 官方 OpenAPI + SigV4 | ✅ 已接入，**用户实测通过** |
| MiniMax Coding Plan | 5h/周用量% + 次数 + 重置时间 | cc-switch 新端点 + 官方 CLI 双保险 | ✅ 已接入，待验证 |
| Kimi / Moonshot | API 余额（代金券/现金明细） | 官方文档 | ✅ 已接入，待验证 |
| 硅基流动 SiliconFlow | API 余额 | cc-switch 同款 | ✅ 已接入，待验证 |
| StepFun 阶跃星辰 | API 余额 | cc-switch 同款 | ✅ 已接入，待验证 |
| OpenRouter | API 余额（总额度/已用） | cc-switch 同款 | ✅ 已接入，待验证 |
| Novita AI | API 余额 | cc-switch 同款 | ✅ 已接入，待验证 |
| Anthropic / OpenAI / Gemini / Qwen | — | 无公开 API Key 式接口 | ❌ 手动维护（调研结论见 README） |
| Kimi 会员编程套餐 | — | 接口存在但 CORS 预检 404 | ❌ 纯前端不可查，手动维护 |

---

## 四、质量与验证证据

- **自动化测试**：160 个用例覆盖脱敏规则、全部 9 家适配器（含签名对拍、错误形状、回退逻辑）、状态评估与排序筛选、存储/日志、备份、确认弹窗、应用壳集成（导航/搜索/主题/危险操作）、悬浮球视图与界面开关。
- **构建**：`vite build` 通过；产物体积 JS gzip ≈ 16.7 kB。
- **真实链路验证**：DeepSeek/智谱/MiniMax/Kimi/火山曾以真实（或占位）密钥在浏览器中实测到官方服务器的真实响应与错误转换；其中 DeepSeek、智谱、火山三家已由用户以真实密钥确认查询成功。
- **安全自查**：界面无明文密钥（首尾 4 位脱敏）、日志结构无密钥字段且错误信息净化、代码仓库无真实密钥、数据仅存本机 localStorage。

## 五、已知限制

1. 纯前端架构：API Key（含火山 SK）存于浏览器 localStorage；刷新依赖页面/迷你窗处于打开状态。
2. Kimi 会员编程套餐因对端 CORS 限制无法自动查询。
3. Anthropic/OpenAI/Gemini/Qwen 无公开的 API Key 式用量接口（调研结论与替代方案见 README）。
4. 各供应商接口为非正式文档端点（但有官方插件/CLI 背书），若上游变更需更新适配器——桌面版的「AI 辅助接口自适配」规划即为解决此问题。

## 六、下一步

1. **近期**：用户对其余 6 家（MiniMax/Kimi/硅基流动/StepFun/OpenRouter/Novita）做真实密钥验证；反馈问题即修复。桌面版日常使用体验反馈（托盘交互习惯、迷你窗尺寸）。
2. **中期（桌面版 Phase 1 完整版，未实施）**：存储层切换（localStorage → 后端 JSON + 密钥入 Windows Credential Manager）+ 迁移向导；托盘图标动态三色/用量角标（需前端经 IPC 上报健康度）；后端定时调度（摆脱 WebView 后台节流）。
3. **远期（已规划未实施）**：按《私有化桌面版演进规划.md》推进 Phase 2（系统通知 + 冷却去重）→ Phase 3（AI 辅助接口自适配）→ Phase 4 界面再升级。

## 七、桌面版（Tauri 壳）运行指南

```bash
# 开发模式（热重载，自动起 Vite dev server）
npm run desktop:dev

# 产出独立 exe（不打包安装器，本机可直接运行）
npm run desktop:build   # → src-tauri/target/release/看额度.exe（model-quota-app.exe 的同内容副本）

# NSIS 安装包（需代理：打包器要从 GitHub 下载 nsis_tauri_utils.dll）
npm run desktop:dist
```

- 双击 exe 即用：主窗口关闭后驻留托盘（左键托盘切换显隐，右键菜单含「迷你小窗」「打开/关闭悬浮球」与「退出」）；重复启动不会开第二个进程。
- 悬浮球：托盘菜单开启后桌面常驻圆形小窗——蓝色=全部正常（显示余额合计），琥珀=有低值/临期提醒，红=有异常（显示需关注数）；单击在球旁展开 300×430 迷你速览窗（鼠标移出自动收起），按住拖动可移位（位置重启保留）；悬停可见逐条告警摘要。
- 数据存储在 WebView2 的 localStorage（按应用标识 `com.modelquota.desktop` 隔离），与浏览器版互不相通，可用设置页「导出/导入 JSON」在两端间迁移。
- 编译环境注意（本机踩坑记录）：需 `~/.cargo/bin` 与 `D:\BuildTools\...\bin\Hostx64\x64` 在 PATH（`~/.cargo/config.toml` 已固定 USTC 镜像与 MSVC 链接器）；图标源文件为根目录 `app-icon.png`，重生成尺寸集：`npx tauri icon app-icon.png -o src-tauri/icons`。
