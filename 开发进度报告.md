# 看额度 · 开发进度报告

> 产品名：**看额度**（原名「模型额度查询」，2026-09-02 更名，功能不变）· 英文名 / 仓库名：**QuotaPeek**（repo 建议 `quota-peek`）
> 仓库：https://github.com/Shzyhao/quota-peek （2026-09-02 首次推送，main 分支）
> 更新日期：2026-09-02
> 项目位置：`model-quota-frontend/`（纯前端，Vite + 原生 JavaScript + Vitest）+ `src-tauri/`（Tauri 2 桌面壳，阶段 8 起生效）
> 配套文档：[模型额度查询要求.md](./模型额度查询要求.md)（原始需求）· [私有化桌面版演进规划.md](./私有化桌面版演进规划.md)（后续规划）· [model-quota-frontend/README.md](./model-quota-frontend/README.md)（技术细节与接口调研）

---

## 一、项目概览

一个轻量级本地小工具，用于查询和记录多个大模型供应商的套餐余额、剩余额度和到期时间。按原始需求文档开发，前期采用「浏览器直连官方接口 + localStorage 存储」的纯前端架构（`model-quota-frontend/`）；阶段 8 起由 Tauri 2 桌面壳（`src-tauri/`）包装为**托盘常驻的桌面应用**，前端代码原样复用。

**核心指标（当前）**

| 指标 | 状态 |
|---|---|
| 自动查询供应商 | **9 家**（DeepSeek / 智谱 Coding / 火山方舟 Coding / MiniMax Coding / Kimi·Moonshot 余额 / 硅基流动 / StepFun / OpenRouter / Novita） |
| 手动维护类型 | 5 类（Anthropic 订阅 / OpenAI·Codex / Qwen / Kimi 会员编程套餐 / 其他） |
| 测试 | **206/206 全部通过**（20 个测试文件，Vitest + jsdom） |
| 生产构建 | `vite build` 通过（前端 gzip 约 58 kB + 按需分包 lib） |
| 用户实测已通过 | DeepSeek、智谱 GLM Coding Plan、火山方舟 Coding Plan（三家真实 Key 验证查询成功） |
| 界面 | 侧边栏应用壳四视图、深浅双主题、用量进度条、搜索筛选、样式化确认弹窗、迷你小窗、备份导入导出 |
| **桌面版（阶段 8 起）** | **Tauri 2 桌面壳已落地**：`src-tauri/target/release/model-quota-app.exe`（约 5.9 MB），托盘常驻 + 关窗驻留 + 托盘迷你小窗 + 单实例，GUI 验收通过 |

---

## 二、开发历程（按阶段）

### 阶段 1 · 初版开发（完成）
- 依据需求文档确定纯前端技术方案：Vite + 原生 JS（无 UI 框架）+ Vitest + jsdom。
- 核心层（`src/core/`）：API Key 脱敏、localStorage 仓储、查询日志（脱敏 + 上限 100 条）、状态评估（到期/低余额/低额度/失败颜色分级）、刷新编排服务。
- UI 层：供应商卡片、添加/编辑弹窗、查询日志表、迷你小窗（`?view=mini`）。
- **DeepSeek 接入**：官方 `GET /user/balance`，实测浏览器 CORS 直连可用，真实 401 错误链路验证通过。
- 验证：64 个测试全绿；浏览器端到端实测（添加→刷新→日志→脱敏）。

### 阶段 2 · 多供应商扩展（完成）
- **智谱 GLM Coding Plan**：采用官方开源插件 `glm-plan-usage` 同款接口 `GET /api/monitor/usage/quota/limit`（裸 Key 鉴权），CORS 实测通过。
- **MiniMax**：官方 CLI `mmx quota` 同款接口 `GET /v1/token_plan/remains`（Bearer），双端点路由（`sk-api-` 普通密钥自动走 `/account/query_balance`）。
- **Kimi / Moonshot**：官方文档接口 `GET /v1/users/me/balance`（Bearer）。
- 完成全行业调研并形成对照表（详见 README）；三家新接口的真实错误路径在浏览器中实测验证。
- 验证：94 个测试全绿。

### 阶段 3 · 演进规划文档（完成）
- 应需求产出《私有化桌面版演进规划.md》：桌面化选型（推荐 Tauri 2）、系统托盘、系统通知、**AI 辅助接口自适配**（声明式 DSL + AI 修订配置的安全设计）、UI 升级方向、四阶段路线图与风险对策。此阶段仅产出分析文档，未实施开发。

### 阶段 4 · 界面样式优化与功能开发（完成）
- **应用壳重构**：单页长滚动 → 左侧边栏 + 四视图（总览/供应商/查询日志/设置）hash 路由。
- **深色模式**：设计 tokens 双主题，跟随系统/浅色/深色三档，顶栏一键切换，迷你窗同步。
- **总览页**：余额合计、供应商数、需关注数、最近刷新统计卡；卡片按严重度排序。
- **供应商页**：关键词实时搜索（局部刷新保焦点）+ 六档状态筛选。
- **用量可视化**：Coding Plan 卡片 5 小时/本周/本月 MCP 三条进度条（<80 绿 / 80–90 橙 / ≥90 红），余额型剩余额度占比条。
- **样式化确认弹窗**：替代原生 confirm，危险操作红色样式，防注入（纯文本渲染）。
- **数据备份**：JSON 导出/导入（覆盖式导入带二次确认与结构校验）、清空全部数据。
- 验证：125 个测试全绿；浏览器 GUI 逐项验证（导航/搜索双向过滤/深色截图经视觉模型确认/确认弹窗）。

### 阶段 5 · 用户实测反馈修复（完成）
用户真实验证发现：智谱只显示 5 小时用量，无本周/已用/总额。根因与修复：
- 智谱周额度与 5 小时额度是**同类型（TOKENS_LIMIT）双条目**，靠 `unit` 字段区分（3=5 小时、6=周）——原适配器只取了第一条。已按 unit 精确解析。
- 接口其实返回**已用/总额数值**（`currentValue`/`usage`）与**重置时间**（`nextResetTime`），现均已解析：卡片显示「本周已用/本周总额」数值，套餐详情行显示重置时间。
- 验证：127 个测试全绿（新增 unit 区分、无 unit 回退、百分比超 100 截断等用例）。

### 阶段 6 · 火山方舟接入（完成，用户已验证通过）
- 调研结论：火山用量查询走**官方 OpenAPI** `POST open.volcengineapi.com/?Action=GetCodingPlanUsage`，鉴权为 **IAM AK/SK + SigV4 签名**（非模型推理的 Ark API Key）；实测 CORS 完全开放。
- 实现：浏览器端 **Web Crypto SigV4 签名**（派生链 Date→Region→Service(ark)→request），测试中与 Node 官方 crypto 实现对拍**逐字节一致**；未订阅 Coding Plan 时自动回退 `GetAFPUsage` 查 Agent Plan。
- **双凭证支持**：表单按类型显示「AccessKey ID + Secret Access Key」两栏，全链路（存储/脱敏/日志净化）支持。
- 安全建议已写入 README：强烈建议使用仅授予方舟只读权限的 IAM 子账号密钥。
- 验证：139 个测试全绿；用户以真实 AK/SK 验证**查询成功**。

### 阶段 7 · 参考 cc-switch 的扩展（完成，待用户验证）
调研 [cc-switch](https://github.com/farion1231/cc-switch) 的用量查询实现，成果：
- **新增 4 家余额查询**（CORS 全部实测开放，cc-switch 同款接口）：硅基流动（`/v1/user/info`，国际站自动 USD）、StepFun（`/v1/accounts`）、OpenRouter（`/api/v1/credits`，余额=总额度−已用）、Novita（`/v3/user/balance`，单位 0.0001 USD 已换算）。
- **MiniMax 双端点升级**：优先 cc-switch 同款新端点 `openplatform/coding_plan/remains`（取 general 条目、周额度激活判断、重置时间），失败自动回退官方 CLI 旧端点。
- **Kimi 会员编程套餐**：查到 cc-switch 所用接口 `api.kimi.com/coding/v1/usages`，但实测**服务端不响应 CORS 预检（OPTIONS 404）**，纯前端无法接入；类型保留为「暂不可查」手动维护。
- 验证：147 个测试全绿。

### 阶段 8 · Tauri 2 桌面壳落地（完成，按《私有化桌面版演进规划》Phase 1 轻量版，2026-09-02）
复用本机 Claude Code 开发「轻析 LiteAI」（`D:\TEST Code\AI 文件分析助手`，Tauri 2 + Vite）的工程模板与本机编译环境经验（VS Build Tools 在 D 盘 + MSVC 链接器固定 + cargo USTC 镜像），零改动嵌入现有前端：
- **新增工程结构**：项目根 `package.json`（Tauri CLI）+ `src-tauri/`（Cargo 工程，仅 3 个依赖：tauri[tray-icon] / single-instance / tauri-build）+ `scripts/gen-icon.mjs`（纯 Node 生成 1024px 应用图标：深色圆角底 + 绿/橙/红三柱条，语义为用量状态）。前端 `model-quota-frontend/` **一行未改核心逻辑**，仅 `main.js` 增加 4 行支持 `#mini` 哈希入口（Tauri 内嵌资产下查询参数会被剥离）。
- **托盘常驻**（需求 11 桌面化）：系统托盘图标 + tooltip + 右键菜单（显示主窗口 / 迷你小窗 / 退出）；左键单击切换主窗口显隐；点击「迷你小窗」打开 400×640 的「额度速览」窗口（复用前端 mini 视图）。
- **关窗驻留**：关闭主窗口 = 隐藏到托盘（WebView 存活，页面内定时刷新不中断）；重复启动 exe 由单实例插件唤起已有窗口；托盘菜单「退出」才真正结束进程。
- **产物**：`src-tauri/target/release/model-quota-app.exe`（约 5.9 MB，opt-level=s + LTO + strip），`--no-bundle` 规避 NSIS 打包器需访问 GitHub 的问题；NSIS 安装包配置已就绪（`npm run desktop:dist`，需开代理）。
- **GUI 逐项验收通过**（2026-09-02，真实运行 + 截图/无障碍树/键盘导航验证）：主窗四视图渲染 ✓ 关窗驻留进程存活 ✓ 单实例唤起 ✓ 托盘菜单弹出与响应 ✓ 迷你小窗内容与空状态引导 ✓ 托盘左键显隐切换 ✓ 深浅主题 ✓。前端 147 测试在改动后复跑全绿。
- 已知边界（与规划的差异）：托盘图标暂为静态色（动态三色/角标需后端感知健康度，归入 Phase 2 与系统通知同批）；隐藏窗口时 WebView2 会节流后台定时器（最低约 1 次/分钟）；密钥仍存 WebView localStorage（Phase 1 完整版的「Credential Manager 迁移」未动，见规划第 5 章）。

---

### 阶段 9 · 界面溢出修复与数值可读性（完成，2026-09-02）
用户实测反馈「添加供应商后，五小时额度/余额等数值超出灰色背景框、显示不全」，浏览器注入长数值数据复现并修复：
- **指标区 4 列改 2×2**：卡片四个指标（余额/剩余额度/已用/总额 或 5h/本周用量/已用/总额）改为两行两列，单个灰框宽度翻倍，长数值（如 155,000,000）完整显示；数值改为超长时可在数字内换行兜底，标签加省略号。
- **formatNumber 千分位分组**：999876543210 → 999,876,543,210（测试断言同步更新为分组格式）。
- **进度条百分比取整**：推导出的剩余占比保留 1 位小数（99.9876543210% → 99.99%→显示 99.99？实际按 1 位小数 99.99% 归一为 100% 或 99.9%），数值列加宽 48→52px。
- **剩余额度条颜色分档修正**：原逻辑把「剩余占比」当「已用占比」分档，导致剩余 99.99% 显示红色；现按已用（100−剩余）分档，主窗与迷你窗同步修正。
- 验证：浏览器注入三家长数值供应商（Coding Plan/余额型）截图逐项确认 ✓；147 测试全绿（`--no-file-parallelism` 串行跑，本机多进程并行时 vitest worker 偶发资源性崩溃）；桌面版重新构建并启动验证。

### 阶段 10 · 低额度弹窗提醒 + 仓库 README（完成，2026-09-02）
- **低额度弹窗提醒**（新功能）：设置页「提醒阈值与定时刷新」卡片新增开关；开启后，每次刷新（手动 / 单个 / 定时）完成时检测 warn/error 级供应商（余额过低、剩余额度过低、用量 ≥ 阈值、临期 / 到期、查询失败），**只对新出现的告警**弹窗提示（红色警示样式，复用样式化确认弹窗，列出供应商与原因）；同一告警同会话只提醒一次，恢复正常后重新计数。实现：`status.js` 新增 `collectAlerts()` 纯函数 + `alertPopup` 设置项，`app.js` 刷新流程接入去重弹窗。
- **仓库 README**：项目根新增 README.md（功能清单、运行指南、安全说明、目录结构、已知限制）。
- 验证：新增 3 个测试用例（collectAlerts 单测 + 弹窗全流程集成测试：默认关不弹→开启弹窗→去重→恢复后重新计数），**150/150 全绿**；浏览器注入告警数据实测弹窗内容与样式（截图确认）；桌面版重建并启动验证。

### 阶段 11 · 桌面悬浮球 + 迷你速览窗（完成，2026-09-02 ~ 09-03）
桌面常驻悬浮球与迷你速览窗两轮迭代（第二轮：圆形科技化改版）：

**第一轮（09-02）· 悬浮球骨架**
- **悬浮球**：无边框置顶小窗（独立 WebView 入口 `#ball`），数据取共享 localStorage；单击在球旁展开迷你窗、按住拖动移动位置（移动 >6px 判定为拖动，调 `startDragging`）；位置持久化（app_config_dir/shell.json），重启还原；托盘菜单「打开/关闭悬浮球」开关。
- **迷你窗浮出模式**：悬浮球单击展开的迷你窗自动置顶 + skip_taskbar，鼠标离开窗体自动收起；托盘菜单打开的为手动模式不自动收起。
- **球内容优先级**：告警数 > 余额合计（≥1 万压缩 `x.x万`）> 供应商数。

**第二轮（09-03）· 圆形科技化 + 低值配色（本轮）**
- **圆形科技风球体**：60px 圆形玻璃质感球（高光渐变 + 状态色内衬 + 深空底），外圈 conic-gradient 旋转光环（6s/圈）、呼吸辉光动画；hover 放大增亮；`prefers-reduced-motion` 全局降级。
- **低值分级配色**（需求：低值显示对应颜色）：球体按最严重告警级别变色——异常红 / 仅低值琥珀 / 正常科技蓝；告警时原生 tooltip 逐行列出供应商与原因；金额 ≥1 亿压缩 `x.x亿`，长数字自适应降字号（`.long` 类），尾零修剪（9218.70→9218.7）。
- **迷你窗缩小**：400×640 → **300×430** 无边框自绘标题栏（渐变 logo + 刷新 + ✕ 关闭=隐藏）；列表紧凑化（行高/字号/间距下调），状态文字与行边框按级别着色，更新时间压缩 `MM-DD HH:mm`；关闭按钮前端直调 `getCurrentWindow().hide()`（capability 新增 `core:window:allow-hide`）。
- **修复**：悬浮球位置恢复 bug——保存的物理坐标曾被 `builder.position` 当逻辑坐标使用，越界后球掉到级联默认位 (128,128)；改为 build 后 `set_position(PhysicalPosition)` 归位。验证机 DPI 虚拟化下 Tauri 物理坐标与 GetWindowRect 有 1.25 换算层，但「Moved 保存 → set_position 恢复」在同一坐标系内自洽，重启落点稳定。
- **清理**：托盘悬停浮窗死代码（tray-icon 在 Windows 无 hover 回调，Enter/Leave 分支永不触发）与 `pointer_inside_mini` 删除。
- 验证：Vitest 16 文件 **158 用例全绿**（新增球三级配色 2 例）；`cargo check` 干净；release 重建后浏览器注入三态数据（error/warn/ok）截图逐项确认球体配色、光环、迷你窗布局与标题栏 ✓；桌面端运行截图确认渲染 ✓。

**已定案结论（勿重排查）**
1. 托盘悬停浮窗在 Windows **不可行**：tray-icon 底层只上报点击、无 hover 回调（文档限制）。
2. ~~透明 WebView 窗口收不到鼠标输入~~ **已推翻（09-03 第三轮验证）**：Tauri 2 Windows 透明窗口默认正常接收输入——`transparent(true)` 只影响渲染不影响命中测试（调研：tauri#13070 反而抱怨透明窗捕获输入、docs.rs `set_ignore_cursor_events` 默认不忽略、iPet 桌宠等透明交互成功案例）。旧结论是合成输入测试假象。
3. 球单击/拖动逻辑**已验证可用**（09-03）：PowerShell 合成点击球中心（GetWindowRect 物理坐标 + mouse_event），「额度速览」迷你窗成功弹出——事件链路（WebView DOM → emit → Rust 建窗）全通。

**第三轮（09-03）· 透明窗口定案**
- 悬浮球改为 `transparent(true)` 真透明窗口 + CSS 画布全透明，**彻底消除不透明方案的深色方块底**；球体、光环、辉光直接叠在桌面上。
- 视觉三轮迭代（see-coder 多模态子智能体像素级验收）：① 透明后黑底消除，但辉光渐变外圈残留 3% 蓝色 alpha，浅色桌面见淡蓝矩形轮廓 → 渐变 62% 处归零；② 顶部仍有辉光被窗口边缘直线截断的硬边（外层 `box-shadow 28px/36px` 扩散延伸到窗口矩形边界）→ 砍掉长阴影、主辉光全部由已收敛的径向渐变承担；③ 复验通过：四方向辉光自然径向渐隐、球外完全透明、无任何矩形轮廓/截断边。
- 输入链路验证：PowerShell 合成点击球中心 → 「额度速览」迷你窗成功弹出（DOM → emit → Rust 建窗全通）。
- 经验沉淀：**透明窗口的视觉效果（阴影/辉光）必须收敛在窗口矩形之内**，任何溢出到边缘的渐变都会被裁出生硬直线；透明不影响命中测试（点击/拖动默认可达 DOM）。

---



### 阶段 12 · 悬浮球界面开关 + 默认开启（完成，2026-09-03）
用户需求：悬浮球除托盘菜单外提供更直观的开关入口，且默认开启。
- **主界面顶栏按钮**：主题按钮右侧新增圆形图标按钮（仅桌面壳 `__TAURI__` 环境渲染，网页版不显示）；开启时高亮（primary-soft 底 + primary 边框），title 提示当前状态。
- **设置页开关**：设置 →「外观」卡片新增「桌面悬浮球」开关（setting-toggle 样式，同样仅桌面版显示），文案说明三色语义与交互。
- **默认开启**：无 shell.json 的首次运行悬浮球自动出现（`load_prefs` 读不到配置时 `visible: true`）；存量用户按已保存偏好。BallPrefs 字段加 `serde(default)` 保证旧文件兼容。
- **事件协议**：前端 `set-ball`（payload: bool，绝对语义）→ Rust `set_ball`（与当前状态不同才 toggle）；任何路径的开关变化都 `emit('ball-state-changed', visible)` 广播，主窗口启动时发 `ball-state-request` 拉取当前状态，顶栏按钮与设置页开关双向同步。
- 验证：Vitest **160 用例全绿**（新增 2 例：网页版不渲染开关、桌面版状态广播同步与 set-ball 发射）；UIA 真机验证（合成鼠标点击驱动不了 WebView 按钮，改用 UIAutomation Invoke/Toggle）：顶栏按钮关闭→球消失且 shell.json `visible:false` ✓ 再开→恢复 ✓；设置页 CheckBox 双向 Toggle ✓；删 shell.json 冷启动默认出现 ✓；顶栏高亮态截图确认 ✓。
- 附注：这台验证机上合成鼠标点击（mouse_event）无法驱动 WebView2 内的按钮（无论前台与否；点击透明悬浮球本身曾成功，可能与置顶未激活路径不同），UIA Invoke 可靠——后续 GUI 自动化验证优先 UIA。

### 阶段 13 · 低额度系统通知（完成，2026-09-04）
低额度提醒从界面弹窗升级为**三选一**：''=关闭 / 'popup'=界面弹窗 / 'notify'=系统通知（Windows 原生 Toast）。
- **实现**：`tauri-plugin-notification` 官方插件，由 **Rust 端发通知**（前端 emit `show-notify` → Rust listener → `NotificationExt` 发 Toast）——绕开前端插件 JS 绑定（withGlobalTauri 不注入插件 API）与权限细节；通知静默失败时错误详情落盘 `app_config_dir/notify-error.txt` 便于诊断。
- **设置迁移**：`alertPopup(bool)` → `alertMethod('popup'|'notify'|'')`，`loadSettings` 一次性迁移旧数据（存储已有 alertMethod 后以新值为准）；设置页原开关替换为下拉（关闭/界面弹窗/系统通知）；新用户默认界面弹窗。
- **网页版回退**：浏览器版选「系统通知」自动回退界面弹窗（无 `__TAURI__` 时）。
- **版本 0.3.0**；测试 **162 例全绿**（新增：notify 分支 emit 断言、迁移三态用例、网页版回退）。
- **真机验证（UIA/computer-use 无障碍全流程）**：a11y 填表添加假密钥供应商（NotifyTest×4）→ 设置切「系统通知」→ 一键刷新 → 查询失败告警产生且**无界面弹窗**（notify 分支生效）→ Rust `show()` 返回 **Ok**（系统接受、无 AUMID 错误，notify-error.txt 不存在）。**横幅未在屏幕显示**：验证机处于锁屏/勿扰状态，Windows 抑制横幅只入通知中心——属系统通知设置行为，非代码问题（README 已附排查提示）；测试供应商与提醒方式设置验证后已还原。
- **构建坑记录**：① rustc 编译 `windows-0.61` crate（通知插件依赖）偶发 `STATUS_STACK_BUFFER_OVERRUN` 崩溃 → `RUST_MIN_STACK=16777216` + `CARGO_BUILD_JOBS=1` 解决；② **直接 `cargo build --release` 会产出指向 devUrl（localhost）的废 exe**——devUrl→frontendDist 的切换由 tauri CLI 构建时注入，务必用 `npm run desktop:build`。

---

### 阶段 14 · v0.5.0：后端定时调度 + 托盘动态图标 + 密钥入凭据管理器 + 桌宠拟人化告警（完成，2026-09-13）

四项功能一次落地（对应《私有化桌面版演进规划》Phase 1 完整版的「后端调度/托盘动态图标」与安全架构收尾，以及 v0.4.0 桌宠形态的告警升级）：

- **后端定时刷新调度**：Rust 常驻线程持有时钟（配置持久化 `refresh.json`，`set_refresh_schedule` 命令由设置页同步；Condvar+序号实现改配置即重算下一轮），到点 emit `backend-refresh-due`，主窗（WebView 常驻，**IPC 事件不受隐藏窗口定时器节流影响**）执行既有 JS 刷新编排——9 家适配器零改动。托盘菜单新增「立即刷新」走同一事件通道。网页版仍用 setInterval（无 `__TAURI__.core` 时自动回退）。
- **托盘动态图标**：前端按全局健康度绘制 32×32 RGBA（底色 绿=正常/琥珀=提醒/红=异常/灰=无数据，白字角标=用量型供应商的最高用量%，多行摘要 tooltip ≤127 字符），经 `update_tray_status` 命令更新托盘；触发点覆盖主窗/迷你窗每次刷新、增删改供应商、导入备份与启动。纯函数 `computeTrayStatus` 单测覆盖，Canvas 绘制桩测。
- **额度密钥入 Windows Credential Manager**：新增 `quota_secret_set/get/has/delete` 命令（复用 `KeyringStore`，service `com.modelquota.desktop`，条目 `quota_key:<providerId>` 一条存齐 apiKey+apiSecret）。本地记录只留 `hasSecret` 标记零明文；启动时（主窗）与备份导入后自动迁移存量明文（单条失败保留下次重试）；刷新经 `resolveSecrets` 注入解析（浏览器版回退记录明文）；删除/清空同步清理凭据条目；对话「供应商导入」改从凭据管理器读 Key；卡片密钥行显示「已存入系统凭据管理器 🔒」；桌面版备份文件不再含密钥（设置页提示同步更新）。
- **桌宠拟人化告警**：提醒方式新增「桌宠播报」档（关闭/界面弹窗/系统通知/桌宠播报）。新告警 emit `pet-speak`（lines=供应商+原因），恢复（从告警集合消失且供应商仍在）emit recoveries 报喜——桌宠气泡显示台词（15s）+ 随机小动作；桌宠未开启时有新告警回退界面弹窗，仅恢复则静默（popup/notify 行为不变）。
- 版本 **0.5.0**（package.json / tauri.conf.json / Cargo.toml 三处同步）；前端测试 **206/206 全绿**（新增 31：secrets 5、trayStatus 8、service 解析注入 2、桌宠播报 3、既有回归）；`cargo check` 干净。

---

### 阶段 15 · v0.5.1：整体 bug 排查修复（完成，2026-09-13）

对 v0.5.0 四项新功能与既有代码做整轮排查（bug + 性能），修复以下问题：

- **P1 · 编辑凭据管理器供应商被表单拦截（v0.5.0 回归）**：表单校验/占位符/`effectiveSecrets` 只认本地明文 `existing.apiKey`，密钥迁移后为空串 → 编辑已迁移供应商不重输密钥无法保存、测试链接不可用。修复：`hasStoredKey/hasStoredSecret`（明文或 hasSecret 标记任一成立），占位符显示「已存入系统凭据管理器」，测试链接经注入的 `getStoredSecrets` 从凭据管理器解析密钥。
- **P1 · 凭据管理器写入失败静默丢密钥**：`saveProviderFromForm` 吞掉 writeSecret 错误仍标记 hasSecret → 用户刚输入的密钥彻底丢失。修复：失败回退明文本地保存（宁明文不丢数据）；浏览器分支同时修正 hasSecret 保留语义。
- **P2 · 备份导入留孤儿凭据条目**：整表覆盖后已删除供应商的 `quota_key:` 条目残留在凭据管理器。新增 `cleanupOrphanSecrets`（导入前快照 id、导入后 diff 清理）。
- **P2 · 「界面弹窗」提醒在后台定时刷新时蒸发**：弹窗弹进隐藏主窗无人可见且被去重不再补发。修复：弹窗前查窗口原生可见性（**真机实测：Tauri 隐藏窗口时 `document.hidden` 仍为 false，必须用 `getCurrentWindow().isVisible()`**），不可见自动升级为系统通知；可见时行为不变。
- **P3 · 启动即弹存量告警**：v0.5.0 在启动时调用 afterDataChange 连带 maybeAlert。修复：启动只初始化托盘图标。
- **P3 · 改无关设置重置刷新倒计时**：任意 `[data-setting]` 变化都重发 set_refresh_schedule（seq++ 重算）。修复：前端调度签名守卫（值没变不重发，失败回滚可重试）+ Rust 侧同值幂等跳过；顺带修复调度线程启动未武装隐患（refresh.json 已启用但前端未同步时不倒计时）。
- **加固/性能**：`quota_secret_set` 拒绝空 providerId；mini/ball 窗 30s 重绘在 `document.hidden` 时跳过。
- 测试 **212/212 全绿**（新增 6：编辑不拦截、测试链接用已存密钥、启动不弹窗、隐藏升级通知、可见时仍弹窗、孤儿清理）；CDP 真机验证空 id 拒绝 / B1 编辑保存 / isVisible 隐藏语义。

---

### 阶段 16 · v0.6.0：多会话 + 桌宠气泡三件套（完成，2026-09-13）

用户立项四功能（气泡/历史×10/会话传文件/Agent），按评估排期先做前两项；本阶段落地：

- **多会话**：`mqc.chat.sessions`（按最近活跃排序）+ `mqc.chat.activeSession`，上限 10 个超出淘汰最旧，每会话消息仍限 100 条；会话标题取首条用户消息截断 18 字；旧版单会话缓冲一次性迁移；对话页工具栏新增会话下拉 +「＋新对话」「删除会话」，主窗与桌宠面板经 storage 事件实时同步（流式中忽略，结束后再取）。
- **桌宠状态播报**：主窗/面板发起对话 emit `pet-chat-status`（thinking/replied/error），桌宠气泡依次显示「让我想想哈···」（点点动画）→「好啦，回复好啦～✨」/「呜…好像出错了」；桌宠正在拖放分析/换装时不插话。
- **主动搭话**：`core/chatter.js` 纯函数文案池（时段问候 + 额度状态感知：告警软提醒/最忙用量/余额合计/无数据引导，语气拟人化）；桌宠每分钟 tick，最小间隔 10 分钟 + 25% 概率节流（约 40 分钟一句），勿扰时段 23:00–08:00 静音，设置页「桌宠形象」卡新增开关（`mqc.pet.chatter`）。
- 版本 **0.6.0**；测试 **228/228 全绿**（新增 16：多会话模型 9、chatter 7、chatView 会话 UI 2——含旧数据迁移）；CDP 真机验证：播种/新建/删除/重载持久化、思考中与回复气泡、设置开关；对话页工具栏单行布局截图确认。

### 阶段 17 · v0.6.1：悬停互动（完成，2026-09-13）

用户需求：鼠标放在形象上时也要有对话（类似主动搭话）。

- **悬停互动**：`core/chatter.js` 新增 `pickHoverLine`——短反应台词池（摸摸/戳戳/邀请互动）为主，30% 概率顺带报最忙用量（≥80% 时转为提醒）；用户主动触发，勿扰时段也回应。pet.js：mouseenter 后 ~0.9s 触发（快速划过不说话），mouseleave 取消；独立 2 分钟冷却；与主动搭话共用开关（`mqc.pet.chatter`）；拖动/流式分析/换装/菜单打开时不插话。
- 版本 **0.6.1**；测试 **232/232 全绿**（新增 4）；CDP 真机验证：合成 mouseenter → 气泡说话 ✓、冷却期内再进入不重复 ✓（截图确认气泡渲染）。
- **v0.6.2 调整（用户反馈：2 分钟冷却太久，「说完就不行了」）**：悬停冷却 2 分钟 → **5 秒**（与 6s 气泡停留衔接，说完离开再进来就能接着聊），并加「不重复上一句」（连抽三次避重）。CDP 验证连续三轮悬停台词各不相同（在呢在呢/被我盯上了吧/嘻嘻好痒）。测试仍 232/232 全绿。

### 阶段 18 · v0.6.3：应用大图标统一（完成，2026-09-13）

用户反馈：托盘小图标已动态变色，但应用大图标（任务栏/窗口/exe）还是旧设计。两项调整：

- **任务栏大图标动态联动**：`update_tray_status` 命令在更新托盘的同时对主窗 `set_icon` 同款 RGBA——任务栏/Alt-Tab 大图标与托盘小图标实时同色同态（红色告警态任务栏实测截图确认）。
- **静态 exe 图标重绘**：`gen-icon.mjs` 从「深色底 + 绿/橙/红三柱」改为「品牌绿渐变底 + 白色三柱」（emerald-400→600 渐变），与托盘动态图标的「绿底白内容」同一视觉语言；`npx tauri icon` 重生成全套（ico/png/NSIS 资产）。
- 版本 **0.6.3**；测试无涉前端逻辑（232/232 维持）；任务栏图标红/白/绿三态实测截图验证。
- **v0.6.4 修正（用户反馈「图标变纯绿了」）**：根因是验证时手动推送的纯色测试 RGBA 覆盖了真实图标（验证后未触发真实刷新恢复，重启即恢复）。顺带补设计缺口：`drawIcon` 在无用量数据（纯余额型供应商、无角标数字）时改为画白色三柱条（与应用图标同款 motif），动态图标永不为纯色块；任务栏恢复实测绿底白字「36」。测试 232/232 维持。

### 阶段 19 · v0.6.5：动态图标改版 ZK 标识（完成，2026-09-13）

用户定调：动态图标底色继续按健康度变化，但不再显示用量数字/柱条，统一显示白色「ZK」品牌标识。

- `computeTrayStatus` 去掉 badge 计算（tooltip 摘要保留）；`drawIcon` 固定绘制「严重度色圆角方块 + 白字 ZK」（bold 15px 居中）；任务栏/托盘同步生效（v0.6.3 的 set_icon 联动不变）。
- 版本 **0.6.5**；测试 **233/233 全绿**（trayStatus 用例改造：断言 fillText('ZK') 与单次 roundRect）；任务栏实测绿底白字 ZK 截图确认。

### 阶段 20 · v0.7.0：会话附件 + Agent 工具循环（完成，2026-09-13）

用户立项四功能的 ③④ 一批落地（评估见阶段 16 前的方案讨论）：

- **会话附件（③）**：新命令 `chat_read_file` 复用轻析解析器（pdf/docx/xlsx/txt/md/csv/json/代码等 27 类扩展名，GBK 容错；20MB 上限；内容截断 8000 字符并标记 truncated；空提取报错）。对话输入框新增 📎（`__TAURI__.dialog.open` 多选，最多 4 个/条，chips 可移除）；附件随消息存会话（chips 展示）。**上下文保护**：`buildOutgoingMessages` 只让「最后一条」用户消息携带附件正文，历史轮自动替换为「【附件：name（内容已省略）】」占位——防止多轮反复携带大文本。
- **Agent 工具循环（④）**：新增 `src-tauri/src/agent.rs`——自建 OpenAI 兼容 tools 客户端（reqwest 0.12 + rustls，`{base}/chat/completions` 非流式），**单步确认式循环**：模型提议工具 → emit `ToolProposed` 挂起 → 前端确认卡片（批准/拒绝，高危红标）→ `agent_resolve` 执行并回填 `{role:"tool"}` 续跑 → 直至最终回答；上限 8 步。9 个工具：get_current_time / get_system_info / list_directory / read_text_file / open_url（仅 http/https）/ open_path / write_clipboard / write_text_file / run_command（PowerShell，20s 超时，CREATE_NO_WINDOW，管道读输出）。**安全红线**：全部工具调用人工批准（无自动批准）；审计日志落盘 `agent-audit.jsonl`（工具/参数/结果摘要/耗时/是否批准）；密钥不进工具上下文；未知工具名直接拒绝续跑。
- 前端：Agent 开关（`mqc.chat.agent`，🔧 按钮高亮）、工具卡（结果入会话历史可回看）、挂起提议卡（批准/拒绝按钮）、取消按钮分流 `agent_cancel`。
- 版本 **0.7.0**；测试 **239/239 全绿**（新增 6：附件组装/历史裁剪 3、chatView 附件 chips + 解析失败提示 + Agent 开关 3）；CDP 真机 E2E（真模型 DeepSeek）：`chat_read_file` 解析真实文件 ✓；Agent 循环「现在几点」→ 提议 get_current_time → 批准 → 执行 → 最终回答 ✓；拒绝路径「写文件」→ 高危卡 → 拒绝 → 文件未创建 + 审计 approved:false + 模型优雅收场 ✓。

### 阶段 21 · v0.8.0：⚡ 只读自动批准 + 批准整条链（完成，2026-09-13）

用户需求：v0.7.0 Agent 的两个体验增强。

- **⚡ 只读工具自动批准（设置项，默认关）**：`isReadonlyTool`（get_current_time / get_system_info / list_directory / read_text_file）；Agent 开启后工具栏出现 ⚡「只读自动批准」开关（`mqc.chat.agentAutoReadonly`）——开启后只读调用跳过确认卡直接执行，写操作/执行类仍逐个确认；审计日志新增 `auto` 字段（区分显式批准与自动放行）。
- **批准整条链（多步自主）**：确认卡三选一「批准执行 / ⚡ 批准整条链 / 拒绝」——选链式后本任务内后续工具调用由 Rust 直接执行（不再挂起），仍受 8 步上限与取消按钮约束；`agent_resolve` 增加 `approve_chain` 参数，`AgentState.chain_approved` 新任务自动重置。工具结果卡显示 ⚡ 标记（`auto` 字段贯通 Rust→前端渲染）。
- 版本 **0.8.0**；测试 **241/241 全绿**（新增 2）；CDP 真模型 E2E：⚡ 只读自动批准（问时间无确认卡直达回答，审计 auto:true）✓；链式（写文件→读回，一次批准全程自动，文件真实写入、审计 3 条 auto:true）✓；⚡ 标记渲染 ✓。

### 阶段 22 · v0.8.1：整体安全复盘修复（完成，2026-09-13）

全量功能复盘（代码审读 + CDP 真机实证）发现并修复：

- **P1 · Agent 挂起未理睬永久卡死（实证）**：`agent_send` 先查 running 后清 pending，挂起状态下（running=true）新任务永远被拒，重载页面丢失确认卡后只能重启。修复：单飞判定改为「running 且无 pending 才拒绝」，挂起残留直接接管重置（实证回归：挂起→重载→新消息正常执行）。
- **P1 · 多 tool_calls 回填不全致 HTTP 400（E2E 发现）**：模型一次返回多个 tool_calls 时仅执行首个，其余 tool_call_id 无响应 → DeepSeek 拒绝「must be followed by tool messages」循环中断。修复：链式模式执行消息内全部调用；单步模式批准首个、其余回填「已跳过」（PendingCall 记录 skipped_ids）；拒绝时全部回填；系统提示词约束一次只调一个工具。
- **P2 · open_url/open_path cmd 元字符注入**：`cmd /C start` 对无空格参数不加引号，URL 中的 `&` 会被 cmd 当命令分隔符。修复：改用 `explorer.exe` 直开，不经 shell 解析。
- **P2 · 链式模式注入放大**：链中读取的不可信内容可能诱导危险调用自动执行。修复：链式自动执行仅限只读工具（`tool_readonly`），写/执行类降级回确认卡重新提议。
- **P2 · 确认卡 800 字符截断**：长命令看不到全部即可批准。修复：预览上限提至 20k 字符 + 可滚动 + 截断标注；`run_command` 命令长度上限 8000 字符（确保始终完整可见）。
- **P3 · 多窗口会话写入竞态**：主窗/面板交叉发送时整表覆盖丢消息。修复：统一 `appendToActiveSession` 入口，追加前从 storage 重读最新会话态。
- **P3 · 附件撑爆 localStorage**：追加时若会话序列化超 3MB 预算，从最旧的消息起省略附件正文（最新一条保持完整），防静默丢数据。
- 版本 **0.8.1**；测试 **242/242 全绿**（新增 1：附件体积预算）；CDP 回归：P1-1 卡死消除 ✓、HTTP 400 消除 ✓、拒绝/审计正常 ✓。

### 阶段 23 · v0.9.0：桌宠语音对话（点按说话 + 回复自动朗读）（完成，2026-09-14）

用户需求：桌宠加语音按钮/语音模式，能语音对话。方案：级联管线（录音 → OpenAI 兼容 ASR → 复用既有 chat_send 对话链路 → OpenAI 兼容 TTS 朗读），对话大脑零改动，默认预置硅基流动（SenseVoiceSmall 识别免费 + CosyVoice2 合成近零成本），任何 OpenAI 兼容语音服务改配置即换。

- **交互形态（点按说话）**：聊天输入栏新增 🎙 按钮（主窗对话页与桌宠面板一处改动两处生效）——点一下开始录音、再点一下停止并识别、识别文本自动走 `doSend()` 发送；回复完成后自动朗读（🔊 朗读开关持久化，`mqc.voice.config.autoRead` 默认开）。桌宠菜单新增「🎙 语音对话」：打开对话面板并自动进入录音态（`mqc.voice.pendingActivate` 带时间戳标记，挂载自查 + storage 事件双路消费，仅面板窗响应；Rust 新增 `pet-voice-chat` 事件以「仅显示不收起」语义开面板）。
- **Rust 侧（commands.rs +5 命令）**：`voice_secret_set/has/delete`（keyring 条目 `voice_key`，与既有密钥同 service）；`voice_transcribe`（reqwest multipart 上传 WAV → `/audio/transcriptions` → 文本）；`voice_speak`（`/audio/speech` → mp3 base64 回传前端 `<audio>` 播放）；超时 30s/60s，错误信息中文化。Cargo 新增 `base64` 依赖与 reqwest `multipart` feature。
- **前端 core/voice.js（新）**：ScriptProcessorNode 累积 PCM → 线性插值降采样 16k → 16bit 单声道 WAV（识别服务兼容性最稳）；60s 上限自动停止；`speechFriendlyText()` 朗读前清洗 markdown（代码块以"（代码略）"占位，剥标题/加粗/链接/表格符号）；新朗读/新录音/新发送自动打断上一段（简易打断）。
- **麦克风授权**：wry 0.55 / tauri 2.11 未暴露权限处理 API（tauri#14753 开放中），采用 WebView2 浏览器参数 `--use-fake-ui-for-media-stream` 自动授权——注意 `additionalBrowserArgs` 会整体替换 wry 默认值，`--disable-features=msWebOOUI,msPdfOOUI,msSmartScreenProtection` 必须带全（main 静态配置 + ball/panel 运行时创建三处）。麦克风仍受 Windows 系统隐私开关约束。
- **桌宠联动**：chatView 向 `pet-chat-status` 新增 `recording`/`transcribing`/`speaking`/`idle` 状态——桌宠气泡依次显示「🎤 主人请讲」「让我听听」「我来念给你听」，结束自动收起（仅 status 类气泡，不误关低额度播报）。
- **配置 UI**：对话页「模型配置」内新增「语音服务」卡（识别/合成 Base URL、模型、音色、Key 入凭据管理器留空不改、🔊 试听、删除 Key）；`.chat-config` 限高 62% 内滚动（语音卡加高后不把面板输入区挤出视野）。
- 版本 **0.9.0**；测试 **264/264 全绿**（新增 22：WAV 编码/降采样/录音器状态机/朗读清洗/IPC 传参/聊天面板语音交互）；CDP 真机 E2E **11/11 通过**（`scripts/cdp-verify-v090-voice.mjs`，本地 mock 语音服务全链路：麦克风授权→录音态→mock 识别文本自动发送→真实模型回复→mock 合成自动朗读→桌宠菜单入口→面板自动激活录音）；视觉验收通过（录音红态/朗读琥珀态/桌宠菜单五项/面板布局，see-coder 复核）。待用户真机说一句话验证真实语音链路（配置硅基流动 Key 后）。
- 遗留（二期候选）：连续免按键对话（VAD 判停 + 打断）、按住说话、流式分句朗读、Live2D 口型同步、edge-tts 免 Key 音色。

### 阶段 24 · v0.9.1：性能分析与桌宠渲染优化（完成，2026-09-15）

用户反馈"又闪退"，要求性能分析与优化。诊断与结论：

- **无崩溃证据**：Windows 事件日志（Application Error/WER/挂起）、本地崩溃转储（CrashDumps）、可靠性监视器均无 model-quota/桌看 记录；产物完整、实例可稳定浸泡。所谓"闪退"最可能是环境巧合：E2E 验证实例存活期间双击 exe → 单实例插件把新进程转发给旧实例即退（随后 E2E 收尾清场）→ 观感即"闪一下没了"；或构建期间 exe 正被替换时启动。双开转发已验证正常（新进程即退、旧实例弹主窗）。
- **性能量化**（scripts/gpu-attrib.mjs + render-check.mjs，可复跑）：整机内存紧张（rustc 曾分配失败）；应用树总内存 ~524MB（主进程 24MB/0 CPU + 7 个 WebView2 子进程）且浸泡走平无泄漏；主窗隐藏 0 CPU；**GPU 与桌宠渲染进程的持续开销 100% 归因于 Live2D 桌宠常驻渲染**（关桌宠 GPU 进程 14.6%→0.0%，重开复原）。
- **关键修复 · Ticker.shared 限帧**：pixi-live2d-display 的模型自动更新注册在 `PIXI.Ticker.shared`（默认不随 app.ticker 的 maxFPS 走）；本机为 144Hz 高刷屏，旧版模型更新实跑 144fps——现在 shared ticker 显式限 30fps（CDP 实测监听器 30Hz、rAF 145Hz），消除约 4.8 倍的模型更新空转。app.ticker 此前已限 30（沿用）。antialias 关闭（贴图四边形上 MSAA 收益极小），see-coder 确认画质无肉眼退化。
- 剩余成本为 30fps 常驻 Live2D 的固有地板（GPU ~14.6% + 桌宠渲染 ~13% 单核），随桌宠关闭归零，属产品核心特性的合理代价；后续可选做电量感知降帧（二期评估）。
- 版本号维持 **0.9.0**（功能无变化，性能修复直接并入本次发布产物）；E2E 11/11 回归通过；浸泡 6.5 分钟存活、内存走平；两帧像素差分证明动画存活。新增诊断脚本 `scripts/gpu-attrib.mjs`（GPU 成本三段归因）与 `scripts/render-check.mjs`（渲染 CPU 漂移 + 动画存活截图差分）。

### 阶段 25 · v0.9.1：功能菜单移到桌宠左侧延伸条（完成，2026-09-15）

用户反馈点击桌宠的功能菜单"挡住了"。菜单原先撑满桌宠窗底部（窗仅 180×220、人物占满全窗，必然遮挡），改为**点击时窗口向左延伸出 160px 菜单条**，菜单立在左侧不再遮挡人物：

- **Rust `pet_menu_layout` 命令**（lib.rs，需用 PET_W 常量故不放 commands.rs）：展开时 ball 窗向左扩 160 逻辑像素并左移，屏幕左缘空间不足自动换右侧；收起按当前位置右移补偿还原（支持菜单开着拖动后原地还原）。展开期间 Moved 事件跳过位置持久化（窗口原点含菜单条不能当桌宠位置存），收起时置回标记让收起位移存回正确位置；桌宠窗重建时复位标记。
- **前端 pet.js**：菜单展开/收起走布局命令并挂 `.menu-left/.menu-right` 类；命令不可用时回退为旧窗内菜单。拖动只收菜单面板、窗口还原延迟到 mouseup（原生拖动会与 set_position 抢位置、且可能吞 mouseup——click 委托里有 pending 兜底）。
- **样式**：`.menu-left/.menu-right` 把菜单定位到延伸条内（152px 宽），左展开时 canvas `translateX(160px)` 保持人物原屏幕位置。
- 版本 **0.9.1**；验证：菜单专项脚本 `scripts/menu-check.mjs` 5/5（侧别/画布平移 160/条宽 152/五项齐全/收起还原）、语音 E2E 11/11 回归、视觉复核（菜单在左、人物光环翅膀完整可见、无截断挤压）。发布 v0.9.1 Release（资产 ZhuoKan-v0.9.1.exe）。

### 阶段 26 · v0.10.0：日程与待办 + 桌宠到点提醒（完成，2026-09-15）

新增日程记录功能：月历 + 待办清单，到点由桌宠气泡提醒（桌宠未开回退系统通知）。

- **数据层 `core/schedule.js`**：日程项 `{id, title, date, time|null, remindLead, repeat, note, done, createdAt}` 存 `mqc.schedules`（repo 扩展 listSchedules/saveSchedule/saveSchedules/deleteSchedule，clearAll 一并清空）；`normalizeScheduleItem` 校验（标题必填、日期/时间格式、提前量档位 0/5/10/15/30/60 分钟、重复 none/daily/weekly/monthly，全天待办提醒提前量归零）；`expandInstances` 把重复日程展开成窗口内具体提醒实例 `{key: id@dueAt, dueAt, at, …}`（monthly 走 Date 归一化，31 日遇小月顺延）；备份 JSON 纳入 schedules（旧备份无此字段安全降级）。
- **视图 `ui/scheduleView.js`（自挂载组件，主窗页 + 桌宠面板共用）**：月历网格周一起始 42 格（当天高亮、点选日、日程打点上限 3 个 + 计数）、当日列表（完成勾选划线、过期未完成标红、重复角标）、「接下来」跨天最近 5 条、新增/编辑模态表单（时间留空 = 全天待办，联动禁用提醒档位）；删除走样式化确认。
- **提醒时钟在 Rust 常驻线程**（commands.rs，与定时刷新同一套「后端持钟」设计，不受 WebView 隐藏窗口定时器节流）：前端 `syncScheduleReminders` 把 [now−15min, now+7 天] 的实例经 `set_schedule_reminders` 同步过去（启动/增删改/补货三时机，同队列幂等短路）；调度线程 20s 扫描，`schedule.json` 持久化队列 + 已触发键集合（48h 修剪，重启恢复并防重复）；到点判断播报通道——`pet_bubble_available`（ball 窗存在且 pet 形态）→ emit `schedule-due`（同批合并）桌宠气泡播报 + 小动作，否则回退原生 Toast（通知失败落盘 notify-error.txt 同低额度通知）；错过语义：超 15 分钟带 missed 标记补报（气泡文案「错过的提醒」、Toast 前缀「（已错过）」），超 12 小时静默吞掉防唤醒雪崩；队列将空 emit `schedule-queue-low` 请主窗补货（7 天窗口耗尽场景）。
- **接线**：主窗导航加「日程」页 + 首页第四张功能卡（home-cards 改自适应列）；桌宠菜单加「📅 日程」入口 → `panel-schedule` 面板窗（500×620，capabilities 增补窗口，open_panel 互斥修正为多面板批量收起）；主窗/面板经 storage 事件跨窗同步（mountSchedulePage 监听挂自建子节点防重挂载堆积）。
- **桌宠气泡改造（用户反馈迭代）**：气泡不再覆盖人物——显示气泡时桌宠窗经新命令 `pet_bubble_layout` 向上长出 130px 气泡条（顶部空间不足自动改向下），人物画布 CSS 位移保持在原屏幕位置，收起还原（与 `pet_menu_layout` 同一套展开/收起/Moved 持久化跳过模式）；两条窗口布局命令在前端经串行队列排队，避免气泡与菜单互切时交错改窗。气泡底色改米黄偏白（#fdf7e7）配深棕文字，两种主题下均可读；**条高随内容自适应**（用户反馈固定 130px 短气泡下空隙过大，两轮迭代）：气泡宽度 164px 较窄、且人物画布顶部本身有约 62px 固有留白（模型按窗口高 72% 缩放底部对齐）——小气泡直接浮在留白里窗口完全不动，内容超出留白才按超出量向上/向下延伸（前端量高传入、流式变高差量调整，画布位移用 CSS 变量 --bubble-strip 同步），气泡底边恒定离头顶约 10px；**最多显示五行**（max-height 125px，超出内部滚动）。**菜单关闭按钮钉顶**（用户反馈 × 飘走压条目）：菜单改两层结构——固定顶栏（× 常驻右上，不随列表滚动）+ 可滚动列表区（细滚动条），「换装（下一套）」标签缩短为「换装」（提示挪至悬停 tooltip）消除窄栏折行。
- **桌宠面板精简 + 额度速览修复（用户反馈迭代）**：① 桌宠菜单进的「对话/语音对话」面板改为 panel 模式——不渲染任何配置元素（「测试连接」「模型配置」按钮、配置区、**当前模型下拉**均不出现，配置与模型切换只在主窗「对话」页），面板工具栏只留会话切换、＋新对话、清空记录，主体为消息流与输入栏（语音🎙/附件📎/朗读/Agent 保留）；
  **配置区在面板中彻底不进 DOM**（模板级裁剪而非 hidden，引用点全部空守卫）；② **输入栏紧凑化（用户反馈）**：操作按钮统一 32px 图标化（🎙/📎/🔊/➤ 发送/⏹ 停止，文字进 title），输入框占满剩余宽度；③ **Agent 默认化（用户反馈）**：对话默认开启 Agent 模式，输入栏不再有开关按钮，设置移至主窗「设置 · 对话 Agent」——Agent 模式下拉（开启默认/关闭）+ 工具权限下拉（只读工具自动批准默认/全部需确认），实时读 localStorage 即时对所有对话窗口生效；② **修复额度速览点击无反应（历史遗留 bug，v0.9.1 同样复现）**：mini 窗是全应用唯一未显式设置 `additional_browser_args` 的窗口，其参数集与已存在的 ball/panel 窗口环境不一致 → WebView2 静默创建失败（Tauri 注册了窗口但 HWND/内容永远出不来）；补齐 `additional_browser_args(BROWSER_ARGS)` 修复；同时把菜单入口改为 `pet-quota` 事件固定模式打开（不随鼠标移出收起——原飞出语义下迷你窗恰好出现在原菜单位置，鼠标一动即被 mini-pointer-left 收起，表现为"点了没反应"），并等菜单收起完成后再锚定开窗避免读到位移中的窗口位置。诊断辅助：mini 创建失败时错误落盘 `mini-error.txt`。
- 版本 **0.10.0**（三处同步）；**产物名改 ASCII**：copy-exe.mjs 产出 `ZhuoKan.exe / ZhuoKan-v{版本}.exe`（gh release 中文资产名会被剥离、中文进程名会让名字过滤漏杀留下单实例活尸，均实证）。测试 286 个全绿（新增 schedule 模块 21 个：校验/CRUD/重复展开边界/提前量/同步幂等；备份用例扩展；修复重复展开把时刻丢在日期零点的 bug——测试先行抓到）。验证脚本 `scripts/cdp-verify-v0100-schedule.mjs`（月历渲染/面板表单新增/Rust 调度到点/气泡播报/持久化与清理）；气泡条专项：显示时窗口 +163 物理像素、位置精确还原、画布位移生效、see-coder 复检气泡与人物零重叠。

### 阶段 27 · v0.11.0：语音对话独立模块（完成，2026-09-16）

用户反馈语音对话应独立于文字对话。新增独立「语音对话」面板（panel-voice，360×540）：

- **桌宠菜单「🎙 语音对话」直达语音场景**：点开即见大麦克风按钮（自动进入待说状态——菜单入口经 mqc.voice.pendingActivate 标记触发自动开录，消费权从 chatView 移交至新组件 voiceView）；点一下说话、再点一下发送，识别自动发送 → 流式回复 → 自动朗读
- **语音面板纯语音形态**：会话切换（与文字对话共享记录）+ 消息流 + 状态提示 + 大圆麦克风（录音态红色脉动动效）+ 朗读开关；无输入框/附件/配置元素；走纯对话通道（sendChat，不接 Agent 工具循环），模型/语音配置仍在主窗「对话」页维护
- chatView 同步清理：pendingActivate 的 storage 兜底消费与挂载自查移除（避免与语音面板抢标记）；主窗对话页的 🎙 语音输入按钮保留（文字对话的语音输入方式）
- 与其他面板互斥（PANEL_LABELS 四面板批量收起）；capabilities 增补 panel-voice
- 版本 **0.11.0**（三处同步，功能新增升 minor）；测试 290 个全绿（新增 voiceView 4 个：骨架渲染/待激活标记消费与过期不消费/朗读开关持久化）

---

## 三、供应商支持总表

| 供应商 | 查询内容 | 接口来源 | 状态 |
|---|---|---|---|
| DeepSeek | API 余额 | 官方文档 | ✅ 已接入，**用户实测通过** |
| 智谱 GLM Coding Plan | 5h/周/MCP 用量% + 数值 + 重置时间 | 官方插件同款 | ✅ 已接入，**用户实测通过** |
| 火山方舟 Coding Plan | 5h/周/月用量% + 重置时间（Agent Plan 自动回退） | 官方 OpenAPI + SigV4 | ✅ 已接入，**用户实测通过** |
| MiniMax Coding Plan | 5h/周用量% + 次数 + 重置时间 | cc-switch 新端点 + 官方 CLI 双保险 | ✅ 已接入，待验证 |
| Kimi / Moonshot | API 余额（代金券/现金明细） | 官方文档 | ✅ 已接入，待验证 |
| 硅基流动 SiliconFlow | API 余额 | cc-switch 同款 | ✅ 已接入，待验证 |
| StepFun 阶跃星辰 | API 余额 | cc-switch 同款 | ✅ 已接入，待验证 |
| OpenRouter | API 余额（总额度/已用） | cc-switch 同款 | ✅ 已接入，待验证 |
| Novita AI | API 余额 | cc-switch 同款 | ✅ 已接入，待验证 |
| Anthropic / OpenAI / Gemini / Qwen | — | 无公开 API Key 式接口 | ❌ 手动维护（调研结论见 README） |
| Kimi 会员编程套餐 | — | 接口存在但 CORS 预检 404 | ❌ 纯前端不可查，手动维护 |

---

## 四、质量与验证证据

- **自动化测试**：162 个用例覆盖脱敏规则、全部 9 家适配器（含签名对拍、错误形状、回退逻辑）、状态评估与排序筛选、存储/日志、备份、确认弹窗、应用壳集成（导航/搜索/主题/危险操作）、悬浮球视图与界面开关。
- **构建**：`vite build` 通过；产物体积 JS gzip ≈ 16.7 kB。
- **真实链路验证**：DeepSeek/智谱/MiniMax/Kimi/火山曾以真实（或占位）密钥在浏览器中实测到官方服务器的真实响应与错误转换；其中 DeepSeek、智谱、火山三家已由用户以真实密钥确认查询成功。
- **安全自查**：界面无明文密钥（首尾 4 位脱敏）、日志结构无密钥字段且错误信息净化、代码仓库无真实密钥、数据仅存本机 localStorage。

## 五、已知限制

1. 纯前端架构残留：浏览器版（网页）API Key 仍存 localStorage（桌面版 v0.5.0 起密钥已入 Windows Credential Manager，本地零明文）；浏览器版定时刷新依赖页面开着（桌面版由 Rust 常驻线程调度）。
2. Kimi 会员编程套餐因对端 CORS 限制无法自动查询。
3. Anthropic/OpenAI/Gemini/Qwen 无公开的 API Key 式用量接口（调研结论与替代方案见 README）。
4. 各供应商接口为非正式文档端点（但有官方插件/CLI 背书），若上游变更需更新适配器——桌面版的「AI 辅助接口自适配」（规划 Phase 3）尚未实施。
5. 托盘图标角标数字取「最高用量%」，余额型供应商无用量概念时角标可能为空（此时仅以底色表达健康度）。

## 六、下一步

1. **近期**：用户真机验证 v0.5.0 四项新功能（后端调度到点刷新、托盘三色角标、密钥迁移后查询、桌宠播报）；反馈问题即修复。
2. **中期**：用量历史快照与趋势（sparkline / 烧速预测）；开机自启（tauri-plugin-autostart）与全局快捷键；AI 辅助接口自适配（规划 Phase 3）。
3. **远期**：自动更新（tauri-plugin-updater，需代码签名）；AI 用量周报（依赖用量历史）。

## 七、桌面版（Tauri 壳）运行指南

```bash
# 开发模式（热重载，自动起 Vite dev server）
npm run desktop:dev

# 产出独立 exe（不打包安装器，本机可直接运行）
npm run desktop:build   # → src-tauri/target/release/ZhuoKan.exe / ZhuoKan-v{版本}.exe（model-quota-app.exe 的同内容副本；v0.10.0 起产物名改 ASCII——gh release 中文资产名会被剥离、中文进程名会让名字过滤漏杀）

# NSIS 安装包（需代理：打包器要从 GitHub 下载 nsis_tauri_utils.dll）
npm run desktop:dist
```

- 双击 exe 即用：主窗口关闭后驻留托盘（左键托盘切换显隐，右键菜单含「迷你小窗」「打开/关闭悬浮球」与「退出」）；重复启动不会开第二个进程。
- 悬浮球：托盘菜单开启后桌面常驻圆形小窗——蓝色=全部正常（显示余额合计），琥珀=有低值/临期提醒，红=有异常（显示需关注数）；单击在球旁展开 300×430 迷你速览窗（鼠标移出自动收起），按住拖动可移位（位置重启保留）；悬停可见逐条告警摘要。
- 数据存储在 WebView2 的 localStorage（按应用标识 `com.modelquota.desktop` 隔离），与浏览器版互不相通，可用设置页「导出/导入 JSON」在两端间迁移。
- 编译环境注意（本机踩坑记录）：需 `~/.cargo/bin` 与 `D:\BuildTools\...\bin\Hostx64\x64` 在 PATH（`~/.cargo/config.toml` 已固定 USTC 镜像与 MSVC 链接器）；图标源文件为根目录 `app-icon.png`，重生成尺寸集：`npx tauri icon app-icon.png -o src-tauri/icons`。
