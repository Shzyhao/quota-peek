你是一名全栈开发工程师，请帮我做一个轻量级本地小工具，用来查询和记录多个大模型供应商的套餐余额、剩余额度和到期时间。

要求尽量简单，不要做复杂系统

核心功能：

1. 可以添加、编辑、删除供应商配置。
2. 每个供应商配置包含：供应商名称、供应商类型、API Key、Base URL、套餐总额度、已用额度、剩余额度、到期时间、备注、是否启用。
3. API Key 只保存在后端或者Windows凭证中，前端展示时必须脱敏，不能明文显示。
4. 可以手动刷新单个供应商，也可以一键刷新全部供应商。也可以定时刷新。
5. 每个供应商需要显示：余额、剩余额度、已用额度、套餐总额度、到期时间、查询状态、最后更新时间。
6. 对没有官方余额查询 API 的供应商，不要使用爬虫、模拟登录或绕过验证，直接标记为“不支持自动查询”，并允许手动维护额度信息。
7. 页面上需要用简单颜色提示异常情况，例如：余额过低、剩余额度过低、7 天内到期、查询失败。
8. 记录最近查询日志，包含供应商、查询时间、状态、余额、剩余额度、错误信息。
9. 不需要登录系统，不需要权限管理，不需要复杂图表，不需要邮件或 Webhook 告警。
10. 支持本地运行
11. 支持在状态栏中通过小窗口查看额度信息

如果供应商支持官方余额查询 API，请通过后端调用官方 API 查询；如果不支持，则返回 unsupported，并使用手动填写的数据展示。

安全要求：

- 不要在前端明文显示 API Key
- 不要在日志中打印完整 API Key
- 不要把真实密钥写进代码
- 代码尽量简单、清晰、容易运行